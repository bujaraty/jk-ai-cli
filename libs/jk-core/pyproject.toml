[project]
name = "jk-core"
version = "0.1.0"
dependencies = ["platformdirs"]

[tool.hatch.build.targets.wheel]
packages = ["src/jk_core"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
