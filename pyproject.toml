[tool.uv.workspace]
members = [
    "apps/*",
    "libs/jk-core",
]

[tool.uv]
# บอก uv ว่า Root นี้ไม่ต้องทำเป็น package (เพราะมันคือถังเก็บโปรเจกต์อื่น)
package = false

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
